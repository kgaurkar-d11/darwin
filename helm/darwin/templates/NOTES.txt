🎉 Darwin Platform Umbrella Chart Successfully Deployed!

1. Deployment Architecture:
   📦 Chart Type: Umbrella (Database-first deployment)
   🗂️  Subcharts Deployed:
{{- if .Values.datastores.enabled }}
     ✅ Datastores (MySQL, Cassandra, Kafka, Zookeeper)
{{- else }}
     ❌ Datastores (disabled)
{{- end }}
{{- if .Values.services.enabled }}
     ✅ Services (Feature Store applications)
{{- else }}
     ❌ Services (disabled)
{{- end }}

2. Deployment Order:
   1️⃣ Datastores deployed first (hook weight: -5)
   2️⃣ Services deployed after datastores ready (hook weight: 5)

3. Access Information:
   🌐 Feature Store: http://darwin-feature-store.local
   🔧 Feature Store Admin: http://darwin-feature-store-admin.local
   🗄️ Database Credentials:
      - MySQL: root/{{ .Values.global.database.mysql.rootPassword }}
      - Cassandra: {{ .Values.global.database.cassandra.username }}/{{ .Values.global.database.cassandra.password }}
   
4. Platform Management:
   kubectl get all -n {{ .Release.Namespace }}  # View all resources
   helm status {{ .Release.Name }} -n {{ .Release.Namespace }}  # Check release status
   
5. Manage Subcharts:
   # Deploy only databases
   helm upgrade {{ .Release.Name }} . --set services.enabled=false
   
   # Deploy only services
   helm upgrade {{ .Release.Name }} . --set datastores.enabled=false
   
   # Enable all components
   helm upgrade {{ .Release.Name }} . --set datastores.enabled=true --set services.enabled=true
