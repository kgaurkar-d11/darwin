<configuration scan="true" scanPeriod="10 seconds">
    <statusListener class="ch.qos.logback.core.status.NopStatusListener" />
    <property name="LOG_PATH" value="${logging.file.path:-./opt/logs/}"/>
    <property name="LOG_FILE" value="${logging.file.name:-darwin-catalog.log}"/>
    <property scope="context" name="HOSTNAME" value="${HOSTNAME}"/>

    <property name="LOGBACK_PATTERN"
              value="%date %highlight(%+5level) %-26thread %X{dd.trace_id:-0} %X{dd.span_id:-0} [%contextName] %logger{36} %green(%msg) %n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOGBACK_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE}</file>
        <append>true</append>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <fieldName>timestamp</fieldName>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <pattern>
                    <pattern>
                        {
                        "level": "%level",
                        "log_message": "%message",
                        "thread": "%thread",
                        "logger": "%logger{36}",
                        "dd.trace_id": "%X{dd.trace_id:-0}",
                        "dd.span_id": "%X{dd.span_id:-0}",
                        "dd.service": "%X{dd.service}",
                        "dd.env": "%X{dd.env}",
                        "context": "[%contextName]"
                        }
                    </pattern>
                </pattern>
                <arguments/>
                <stackTrace>
                    <fieldName>stacktrace</fieldName>
                </stackTrace>
            </providers>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archived/${SERVICE_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>512MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>15GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <includeCallerData>false</includeCallerData>
        <neverBlock>true</neverBlock>
    </appender>

    <springProfile name="local | testcontainer | test">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="!(local | testcontainer | test)">
        <root level="INFO">
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>

</configuration>