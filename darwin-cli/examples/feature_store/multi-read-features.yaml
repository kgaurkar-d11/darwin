# Multi-read features request configuration
# Used with: darwin-cli feature-store features multi-read --file
requests:
  - feature_group_name: test_engagement_fg
    feature_group_version: v1
    feature_columns:
      - activity_score
    primary_keys:
      names:
        - user_id
      values:
        - ["123"]

