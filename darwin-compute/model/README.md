# Models

Models is a folder that contains common models used in both the App Layer and SDK.

## Folder Structure

    .
    ├── src
    │   └── compute_models  # Common models used in both the App Layer and SDK
    └── tests               # Tests for the models
